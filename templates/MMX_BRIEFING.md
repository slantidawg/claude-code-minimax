# MMX Briefing — `<project-name>`

> This file is auto-prepended to every `mcp__minimax__delegate_task` call in this project. Keep it accurate, terse, and project-specific. Lessons that apply broadly belong in the `delegating-to-mmx` skill, not here.

## Project structure

- **Repo root:** `<absolute path>`
- **Worktrees:** `<if any — list them, why they exist, when to use which>`
- **Language / runtime:** `<Python 3.11 / Node 20 / etc.>`
- **Package manager:** `<uv / npm / cargo / etc.>`

## Definition of done (for any delegation)

A delegated task is "done" when:
- `<command 1>` exits 0
- `<command 2>` exits 0 with `<expected stdout pattern>`
- No new files outside `<allowed dirs>`
- No edits to `<forbidden files>` (e.g., generated artifacts, lockfiles)

## Test runner

```bash
<exact invocation>
```

Test files live at `<path>/`. Tests are categorized by `<convention>`. New tests follow `<naming/placement convention>`.

## Project-specific gotchas

<!-- These are the lessons that bit you, written so a fresh subprocess understands them.
     Examples — replace with the real ones for this project: -->

- **`<tool> <subcommand>` and pull-only refs.** Our compose file uses `image: ghcr.io/...` as a PULL-only reference. `docker compose build` tags the local build with the project-derived name, NOT the registry ref, so `docker compose up` will try to pull and fail unauthenticated. Briefs that involve `docker compose up` must include either a retag step or an image-build-with-tag override.
- **`<framework>` test fixture pattern.** Our `<AuthMiddleware>` tests use a fixture at `<conftest.py:42>` that injects `<X>`. Don't rewrite around it — extend it.
- **`<external system>` query language.** Our `<JQL>` builder has these gotchas: `<list>`. Always paste the exact query string the code produces.

## Files that should rarely be touched

- `<generated/file>` — regenerated by `<command>`; don't hand-edit
- `<lockfile>` — only update via `<package manager command>`
- `<config that has security implications>` — flag to me for review

## What "manual" means

If a brief or plan contains "manual smoke" / "operator verifies" / "you decide if X is acceptable" — that's a placeholder, not a category. Convert it to a runnable command sequence with expected output before delegating.

## When NOT to delegate work in this project

- Anything that requires `<credential that only the operator has>`
- Anything that touches `<production resource>` directly
- Architectural decisions across `<two or more bounded contexts>` — split first

---

Last updated: `<date>` after `<incident or learning>` that prompted the change.
